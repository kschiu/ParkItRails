<%= simple_form_for(@purchase, html: { class: 'form-horizontal' }) do |f| %>
  <% if @purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase.errors.count, "error") %> prohibited this listing from being saved:</h2>

      <ul>
      <% @purchase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :listing_id, :as => :hidden %>
  <%= f.input :seller_id, :as => :hidden %>
  <%= f.input :date %>
  <%= f.input :model %>
  <%= f.input :make %>
  <%= f.input :license %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
