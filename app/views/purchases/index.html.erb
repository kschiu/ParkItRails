<h1>Purchased Listings</h1>

<table>
  <thead>
    <tr>
      <th>Purchase Date</th>
      <th colspan="1"></th>
    </tr>
  </thead>

  <tbody>
    <% @purchases.each do |purchase| %>
      <tr>
        <td><%= purchase.date %></td>
        <td><%= link_to 'Show', purchase %></td>
      </tr>
    <% end %>
  </tbody>
</table>

